#ifndef STARTSCREEN_H
#define STARTSCREEN_H

#include "config.h"

void RenderStartScreen(sf::RenderWindow &window, bool &startScreen, sf::Color &bgColor, std::string &playerName);

#endif  // STARTSCREEN_H