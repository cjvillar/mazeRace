#ifndef ENDSCREEN_H
#define ENDSCREEN_H

#include "config.h"

void RenderEndScreen(sf::RenderWindow &window, bool &endScreen,
                     sf::Color &bgColor, const std::string &fileName);

#endif  // ENDSCREEN_H